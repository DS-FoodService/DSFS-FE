[build]
  publish = "dist"
  command = "npm run build"

# API 프록시
[[redirects]]
  from = "/api/*"
  to = "https://api.babsang.shop/:splat"
  status = 200
  force = true

# SPA 라우팅 지원 - 모든 경로를 index.html로 리다이렉트
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
